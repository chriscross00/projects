---
title: "R Notebook"
output: html_notebook
---
```{r}
library(randomForest)
```

```{r}
df <- iris
```

```{r}
# dataframe
train_ir <- df[, 1:4]
# object, not a vector
test_ir <- df[, 5]
```

```{r}
model <- tuneRF(train, test)
```

```{r}
model
```

